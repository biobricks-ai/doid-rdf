[project]
name = "doid-rdf"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyld>=3.1.0",
    "rdflib>=7.1",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
